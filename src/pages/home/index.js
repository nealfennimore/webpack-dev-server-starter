import React from 'react';
import PropTypes from 'prop-types';
import Button from 'global/components/button';

const Home = ( props ) => (
    <main>
        <Button>
            Home page
        </Button>
    </main>
);

Home.propTypes = {};

export default Home;
